pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stage('build') {
            steps {
                echo "building app"
                echo "Yeah"
            }
        }
    }
}